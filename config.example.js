/* Cloud config — Psyche v22 connected to Supabase.
   Paste your NEW project's values below (Supabase → Project Settings → API).
   The anon/publishable key is safe to ship in a browser; row-level security
   protects your data. Until these are filled in, the app runs fully offline
   (Cloud · off) — no auth, no sync, no stats. */
window.PSYCHE_CONFIG = {
  SUPABASE_URL: "https://YOUR-NEW-REF.supabase.co",
  SUPABASE_ANON_KEY: "YOUR-NEW-ANON-PUBLIC-KEY"
};
